/*mysql 기본 명령어 모음
CREATE : 만들기
INSERT INTO : 데이터 삽입
SELECT : 데이터 조회
WHERE : 조건
UPDATE : 데이터 수정
DELETE : 데이터 삭
JOIN : 테이블 간 연
ORDER BY ASC or DESC : 오름차순,내림차순 정렬
DISTINCT : 중복 제거
COUNT(*) : 개수
AVG(*) : 평균
MAX(*) : 최대 값
MIN(*) : 최소 값
GROUP BY : 그룹
HAVING : 그룹 조건
LIMIT : 조회 개수 제한
BETWEEN : 범위 조건
IN : 여러 값 중 하나
LIKE : 문자열 검색 %, _
AS : 별칭
*/

/*테이블 만들기*/
CREATE TABLE users (
    id INT PRIMARY KEY,
    name VARCHAR(100),
    age INT
);
